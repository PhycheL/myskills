
1.将原始html文件拷贝一个备份。
2.调用 `extract_bookmarks.py` 脚本，抽取具体希望分析的书签分类。
3.连续调用 `check_duplicate_urls.py` 和 `remove_duplicate_bookmarks.py`，将刚才抽取出来的分类进行去重。
4.有很多书签只是url稍有差异，实质的内容都一样的，你能帮我分析一下，有哪些这样类型重复的书签吗？
5.与我讨论分类建议。不要按"内容是什么"分类，要按"我要干什么"分类，主要目标是我更好的使用。多轮讨论，等我明确的认为可以了，然后才能执行。
6.删除过程中的中间文件，只保留原始html的备份和最终输出的分类完成的html书签，这两个文件。